import { View } from 'react-native';
import styled from 'styled-components/native';

export const OverlayStyled = styled(View)`
  background-color: rgba(0, 0, 0, 0.3);
  bottom: 0;
  left: 0;
  position: absolute;
  right: 0;
  top: 0;
`;
